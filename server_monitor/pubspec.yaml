name: server_monitor
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+2

environment:
  # 🔴 关键修改：改为兼容目前主流 Flutter 版本的范围
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  web_socket_channel: ^2.4.0  # 新增 WebSocket 支持

  cupertino_icons: ^1.0.8
  
  # 🔴 关键修改：删除了重复项，保留了较新的版本
  http: ^1.6.0 
  
  shared_preferences: ^2.5.4
  fl_chart: ^1.1.1
  path_provider: ^2.1.5
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.14.4
  flutter_lints: ^3.0.0 # 建议改为 3.0.0 或 4.0.0，6.0.0 可能太新导致不兼容

flutter:
  uses-material-design: true

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon.png"
  min_sdk_android: 21


  